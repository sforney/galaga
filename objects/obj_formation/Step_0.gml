/// @description Insert description here
// You can write your code in this editor

/*
if(cur_timer == 0) {
	cur_timer = move_timer;
	array_foreach(formation_coords, formation_move);
	array_foreach(contents, move_enemy_in_formation);
	array_foreach(obj_game.enemies, move_formation_paths);
	translate_counter += translate_step;

	if(translate_counter >= max_translate) {
		translate_counter = 0;
		if(translate_direction == "right") {
			translate_direction = "left";
		} else {
			translate_direction = "right";
		}
	}
} else {
	cur_timer--;
}
*/



